pipeline {
    agent any

    tools {
        maven 'Maven'
    }

    stages {
        stage('Build') {
            steps {
                sh "${tool 'Maven'}/bin/mvn clean compile"
            }
        }
        stage('Test') {
            steps {
                sh "${tool 'Maven'}/bin/mvn test"
            }
        }
        stage('Deploy') {
            steps {
                sh "${tool 'Maven'}/bin/mvn package"                
            }
        }       
    }
}
